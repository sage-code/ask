{
  "name": "ask-cloudflare-svelte",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "schema": "wrangler d1 execute DATABASE --remote --file scripts/schema.sql --yes",
    "deploy": "npm run schema && wrangler deploy"
  },
  "dependencies": {
    "svelte": "^5.56.4"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "vite": "^8.1.3",
    "wrangler": "^4.107.0"
  }
}
