{
  "name": "ask-cloudflare-svelte",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "schema": "wrangler d1 execute --database-id be7f803f-9df5-4e53-80cb-193f544d26aa --file scripts/schema.sql",
    "deploy": "npm run schema && wrangler deploy"
  },
  "dependencies": {
    "svelte": "^4.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "vite": "^5.0.0"
  }
}
